# Agents

This repo's worker constitution lives in `CLAUDE.md` (read it first — build oracle, hard rules,
map), its shared memory in `harness/LEDGER.md`, and its growth protocol in
`.claude/skills/grow/SKILL.md`. Any AI worker, whatever the tool, follows those three files.
